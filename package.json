{
  "name": "solut-arquive",
  "version": "1.0.0",
  "private": true,
  "description": "Solut_Arquive - aplicativo Android/PWA",
  "scripts": {
    "build": "vite build",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "build:android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "prepare:vendor": "node scripts/prepare-vendor.mjs"
  },
  "dependencies": {
    "@capacitor/core": "8.5.2",
    "@capacitor/filesystem": "8.1.3",
    "jszip": "3.10.1",
    "pptxgenjs": "3.11.0",
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/assets": "3.0.5",
    "@capacitor/cli": "8.5.2",
    "vite": "8.3.0"
  }
}
